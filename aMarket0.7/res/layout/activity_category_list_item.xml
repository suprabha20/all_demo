<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent" 
    android:layout_height="68dp"
    android:background="@drawable/list_item_bg_selector">

    <!-- 分类Icon -->
    <ImageView android:id="@+id/iv_icon" 
        android:layout_width="48dp"
        android:layout_height="48dp" 
        android:layout_margin="10dp"
        android:layout_alignParentLeft="true"
        android:scaleType="fitCenter"  />

    <!-- 分类名 -->
    <TextView android:id="@+id/tv_category_name"
        android:layout_width="wrap_content" 
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/iv_icon"
        android:layout_marginTop="10dp" 
        android:layout_alignParentTop="true"
        android:textAppearance="@style/app_text_style1" />
        
    <!-- 分类应用数量 -->
    <TextView android:id="@+id/tv_app_num"
        android:layout_width="wrap_content" 
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="8dp"
        android:layout_centerVertical="true" 
        android:textAppearance="@style/app_text_style2"/>

    <!-- 分类应用列表 -->
    <TextView android:id="@+id/tv_category_description"
        android:layout_height="wrap_content" 
        android:layout_width="wrap_content"
        android:layout_marginRight="8dp"
        android:singleLine="true" 
        android:ellipsize="end"
        android:layout_toRightOf="@id/iv_icon"
        android:layout_toLeftOf="@id/tv_app_num"
        android:layout_below="@id/tv_category_name"
        android:textAppearance="@style/app_text_style2"  />

</RelativeLayout>